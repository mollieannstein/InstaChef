<% @recipe.procedures.uniq.each do |p| %>
  <div id="<%= p.term.downcase %>">
    <div class="card-title"><a href="<%= p.youtube_url %>"><%=p.term %></a></div>
    <div class="card-subtitle"><%= p.instructions %></div>
  </div>
<% end %>
